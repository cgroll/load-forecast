# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
archive/

# Data files (cached by DVC, not tracked in git)
/data/

# Model artifacts (cached by DVC, not tracked in git)
/models/

# Metrics (cached by DVC, not tracked in git)
/metrics/

# Report outputs (generated by DVC, tracked in git)
# Reports are kept in git because they're generated outputs for sharing

# Temporary notebook files
*.ipynb
